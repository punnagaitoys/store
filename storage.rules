rules_version = '2';

// Emulator-only Storage rules for integration testing (task 1.3).
// Open access applies ONLY to the local Firebase Emulator Suite.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
